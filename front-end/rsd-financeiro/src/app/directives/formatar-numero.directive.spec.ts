import { FormatarNumeroDirective } from './formatar-numero.directive';

describe('FormatarNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatarNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
