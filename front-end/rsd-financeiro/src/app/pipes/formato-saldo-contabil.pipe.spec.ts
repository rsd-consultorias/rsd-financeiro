import { FormatoSaldoContabilPipe } from './formato-saldo-contabil.pipe';

describe('FormatoSaldoContabilPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoSaldoContabilPipe();
    expect(pipe).toBeTruthy();
  });
});
