import { FormatoDataPipePipe } from './formato-data-pipe.pipe';

describe('FormatoDataPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDataPipePipe();
    expect(pipe).toBeTruthy();
  });
});
